## Simple mean app for showing azure regions

To start the app run:
```
cd /azure-map
npm start
```
browser should open localhost:3000

Note: I don't have Azure subscription yet so I have to use location data from someone's else.
