import { ObservableQuery } from 'apollo-client';
export declare class ObservableQueryRef {
    private ref;
    constructor(ref?: ObservableQuery<any>);
    setRef(ref: ObservableQuery<any>): void;
    getRef(): ObservableQuery<any>;
}
